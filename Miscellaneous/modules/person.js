class Person {
    constructor(name, occupation, nationality, age) {
        (this.name = name), (this.occupation = occupation), (this.nationality = nationality), (this.age = age);
    }
}

export default Person;
